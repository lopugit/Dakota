Radio option; group options with a shared `name`.

```jsx
<Radio name="season" label="Spring" checked={s === 'spring'} onChange={() => setS('spring')} />
<Radio name="season" label="Autumn" description="Rounder, sweeter cooking" />
```

- `label` / `description`
- Native input props
