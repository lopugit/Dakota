The signature yin↔yang meter — a marker on the chakra spectrum. −1 is most yin (violet), +1 most yang (red), 0 balanced.

```jsx
<BalanceMeter value={0.2} label="Slightly yang" />
<BalanceMeter value={-0.4} size="sm" showLabels={false} />  // inline, e.g. food rows
```

- `value`: −1…+1
- `label`: italic note between Yin/Yang captions
- `size="sm"`: 4px hairline track for lists
