Ink tooltip on hover/focus; keep labels to a few words.

```jsx
<Tooltip label="Toggle theme">
  <IconButton aria-label="Toggle theme"><Icon name="moon" /></IconButton>
</Tooltip>
```

- `label` / `side`: `top` (default) | `bottom`
